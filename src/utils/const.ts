export const TOKEN_NAME = 'token-todo';

export enum ReducerName {
  Counter = 'COUNTER',
  User = 'USER',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
